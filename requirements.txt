# Argus core is stdlib-only. This file is intentionally empty of requirements.
#
# Optional — the `phone` module uses libphonenumber. Without it that one module
# yields a "not installed" finding and nothing else changes:
#
#     pip install 'phonenumbers>=8.13'
